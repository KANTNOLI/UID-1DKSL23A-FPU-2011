import styles from "./IdeaToWork.module.scss"

function IdeaToWork() {
    return (
        <section className={styles.idea}>
            asdasdas
        </section>
    );
}

export default IdeaToWork;